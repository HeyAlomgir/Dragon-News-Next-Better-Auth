import { monsert } from '@/app/layout';
import React from 'react';

const Login = () => {
    return (
        <div className={`${monsert.className}`}>
            <h1 className='text-3xl'>Login</h1>
        </div>
    );
};

export default Login;